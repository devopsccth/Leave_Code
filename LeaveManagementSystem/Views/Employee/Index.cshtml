@model IEnumerable<Employee>
@{ ViewData["Title"] = "จัดการEmployee"; }

<div class="d-flex justify-content-between align-items-center mb-3">
    <h2>จัดการEmployee</h2>
    <a asp-action="Create" class="btn btn-primary">
        <i class="bi bi-plus-circle"></i> เพิ่ม
    </a>
</div>

<div class="card">
    <div class="card-body">
        <div class="table-responsive">
            <table id="employeeTable" class="table table-striped table-hover">
                <thead>
                    <tr>
                        <th>รหัส</th>
                        <th>ชื่อ-สกุล</th>
                        <th>อีเมล</th>
                        <th>แผนก</th>
                        <th>ตำแหน่ง</th>
                        <th>บทบาท</th>
                        <th>สถานะ</th>
                        <th>การดำเนินการ</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model != null && Model.Any())
                    {
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td><small>@item.EmployeeCode</small></td>
                                <td>@item.FullName</td>
                                <td><small>@item.Email</small></td>
                                <td>@item.DepartmentName</td>
                                <td>@item.PositionName</td>
                                <td><span class="badge bg-info">@item.Role</span></td>
                                <td>
                                    @if (item.IsActive)
                                    {
                                        <span class="badge bg-success">ใช้งาน</span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-secondary">ไม่ใช้งาน</span>
                                    }
                                </td>
                                <td>
                                    <a asp-action="Details" asp-route-id="@item.EmployeeId" class="btn btn-sm btn-info" title="รายละเอียด">
                                        <i class="bi bi-eye"></i>
                                    </a>
                                    <a asp-action="Edit" asp-route-id="@item.EmployeeId" class="btn btn-sm btn-warning" title="แก้ไข">
                                        <i class="bi bi-pencil"></i>
                                    </a>
                                    <form asp-action="Delete" asp-route-id="@item.EmployeeId" method="post" class="d-inline">
                                        <button type="submit" class="btn btn-sm btn-danger" onclick="return confirm('คุณแน่ใจหรือไม่?')" title="ลบ">
                                            <i class="bi bi-trash"></i>
                                        </button>
                                    </form>
                                </td>
                            </tr>
                        }
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        $(document).ready(function() {
            $('#employeeTable').DataTable({
                language: {
                    url: '//cdn.datatables.net/plug-ins/1.13.7/i18n/th.json'
                },
                order: [[0, 'asc']],
                pageLength: 25,
                columnDefs: [
                    { orderable: false, targets: 7 }
                ]
            });
        });
    </script>
}
